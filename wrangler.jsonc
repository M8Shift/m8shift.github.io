{
  // Cloudflare Pages direct-upload configuration.
  // Build first with `npm run docs:build`, then deploy with
  // `npx wrangler pages deploy`.
  "name": "m8shift-site",
  "compatibility_date": "2026-06-22",
  "pages_build_output_dir": "./docs/.vitepress/dist"
}
